# ── Generated output (personal trip data) ────────────────────────────────────

# Personal one-off data-prep scripts (hardcoded drive paths, building names, coords)
tools/backfill_pre2019_locations.py
# Backups of the gitignored coordinate file
locations.json.bak
*.json.bak

# Compressed images — hosted on R2, never in git
hosted-photos/

# Symlinks into hosted-photos/
web/trips/*/thumbnails
web/trips/*/display

# Generated trip metadata (manifests, routes, index)
web/trips/

# Generated collection data (china/rooftopping hubs + site stats; built by
# build_collections.py from config/, includes the gated .all variant)
web/collections/

# Personal trip config (drive paths, public/private trip lists)
scripts/trip_inventory.json
public.json
trips.json

# Curated building/location coordinates (reveals private trip locations)
locations.json

# All config/*.json reveal private trip data (drive paths, building coords,
# classifications, road-trip/roof lists). Ignore the whole config/ dir except
# the checked-in example. The real files are source-controlled separately in
# the private website-configs repo, synced by deploy.py.
config/*.json
config/geo/
!config/trips.example.json

# Generated year/trip HTML pages
web/20*/

# ── Credentials / local config ────────────────────────────────────────────────
.env.deploy
wrangler.toml

# ── Local docs & agent context (personal paths/data, never public) ───────────
docs/
CLAUDE.md
scripts/
.claude/
# Planning + runbook docs describe the privacy/gating model — keep out of the public repo
CHINA_AND_BLOGS_PLAN.md
RUNBOOK.md

safety-check/
incorrect-location-names.md

# Personal one-off reconstruction scripts (drive paths, trip-specific data)
tools/reconstruction/personal/

# ── OS / tooling ─────────────────────────────────────────────────────────────
.DS_Store
__pycache__/
*.pyc
venv/
.venv/
.wrangler/

geotag-photos.kanban.md
# Per-photo privacy: manual overrides + the generated image-proxy access index
# (both list private photo ids/trips — regenerate with ./photo_privacy.py)
config/photo_privacy.json
functions/photos/private_index.json

# Bridge visit session labels (generated by analyze_bridge_visits.py)
config/bridge_visits.json
config/backfill/

# ── Blogs (personal content, never public in git) ────────────────────────────
# Source files (source of truth per post) + registry + tile covers + one-off
# migration scripts (drive paths, export locations) + generated pages/assets
content/
config/blogs.json
config/tile_covers.json
tools/blog_migration/
web/blogs/
web/blogs.html

# Privacy audit galleries (tools/public_photo_audit.py [--private])
_public_photo_audit.html
_private_photo_audit.html

*.txt