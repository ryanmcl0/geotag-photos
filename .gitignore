# ── Generated output (personal trip data) ────────────────────────────────────

# Compressed images — hosted on R2, never in git
hosted-photos/

# Symlinks into hosted-photos/
web/trips/*/thumbnails
web/trips/*/display

# Generated trip metadata (manifests, routes, index)
web/trips/

# Personal trip config (drive paths, public/private trip lists)
scripts/trip_inventory.json
public.json
trips.json

# Curated building/location coordinates (reveals private trip locations)
locations.json

# Generated year/trip HTML pages
web/20*/

# ── Credentials / local config ────────────────────────────────────────────────
.env.deploy
wrangler.toml

# ── Local docs & agent context (personal paths/data, never public) ───────────
docs/
CLAUDE.md
scripts/
.claude/

safety-check/
incorrect-location-names.md

# Specific personal trip reconstruction scripts
reconstruction/guizhou_gpx_build.py
reconstruction/guizhou_road_route.py
reconstruction/hubei_hunan_road_route.py
reconstruction/iceland_italy_gpx_build.py
reconstruction/mauritania_train_gpx.py
reconstruction/geotag_by_raws_dirs.py

# ── OS / tooling ─────────────────────────────────────────────────────────────
.DS_Store
__pycache__/
*.pyc
venv/
.venv/
.wrangler/

geotag-photos.kanban.md